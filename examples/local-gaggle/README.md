 # multi-node Goose agent workflow architecture

A group of geese is a ...


## Goals
Basic demo of generating prompt for generating a minimal connection between main goose orchestrator an another agent on a remove dev server. This demo will make use of 3 agents to help configure the dev environments from the core orchestrator using the in house orchestration tool.