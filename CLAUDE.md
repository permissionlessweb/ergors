# Claude.md

- ALWAYS use `cargo chec` in replacement of `cargo check`. this will decrease the overall character ocount of errors and warning without stripping the core issues. Outputs a JSON array like ["Error (severity 5)...", "Related..."]. No errors? [].
- ALWAYS use `cargo tes` in replacement of `cargo test`. this will decrease the overall character count of tests outputs.  Outputs a JSON array,No errors? [].
- NEVER modify prost generated types manually in `./packages/ho-std/src/types/ergors/gen`. ALWAYS regenerate using `cargo run` in proto directory.
This is a rust-project using our types & clients generated by proto3 definitions in proto for interoperable reproducible libraries. USE ONLY EXISTING TYPES DEFINED IN PACKAGE. ALWAYS GENERATE NEW TYPES WITH PROTO3 DEFINITIONS FOR GENERATIVE VERSIONING CONTROL.

## IMPORTANT LOCATIONS

- Types generated exist in `packages/ho-proto-rs/src/prelude.rs`
- Traits used and defined are in `packages/ho-std/src/traits/mod.rs`
- Traits customized via rs-derive package in `proto/rs-derive/src/lib.rs`

## DEVELOPING

## RUNNING/USING ERGORS

## WORKFLOW PROTOCOL
